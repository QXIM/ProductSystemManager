<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neuedu.dao.OrderItemDao">


    <insert id="addOrderItem" parameterType="list" keyProperty="id" useGeneratedKeys="true">

        insert into orderItem(user_id, orderItem_no,product_id,product_name,product_image,current_unit_price,quantity,total_price,create_time) values
    <foreach collection="list" item="item" separator=",">
    (#{item.user_id},#{item.orderItem_no},#{item.product_id},#{item.product_name},#{item.product_image},#{item.current_unit_price},#{item.quantity},#{item.total_price},now())
    </foreach>

    </insert>

    <!--result


    <select id="findAll" resultMap=""-->



    <!--<resultMap id="cart-product" type="com.neuedu.entity.Cart" autoMapping="true">
        <id property="cartid" column="cartid"></id>


        <collection property="product" column="product_id" javaType="com.neuedu.entity.Product" autoMapping="true">

            <id property="productid" column="productid"></id>


        </collection>


    </resultMap>
    <select id="findAll" resultMap="cart-product">

        select c.num AS cart_no,c.cartid AS id, c.product_id  productid, c.quantity  ,p.pname,p.detail,p.image,p.stock,p.price from cart c  left join product p on c.product_id=p.productid
    select user_id, orderItem_no,product_id,product_name,product_image,current_unit_price,quantity,total_price,create_time from orderItem o left join product p on

    </select>-->



</mapper>