<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.neuedu.entity.OrderItem">




    <insert id="addOrderItem"  parameterMap="java.util.List">

insert into orderItem(user_id, orderItem_no,product_id,product_name,product_image,current_unit_price,quantity,total_price,create_time) values(#{user_id},#{orderItem_no},#{product_id},#{product_name},#{product_image},#{current_unit_price},#{quantity},#{total_price},now())

</insert>
<!--

    insert into jikeUser (userName, password) values (iser,psw) ,
    insert into jikeUser (userName, password) values
    <foreach item="item" index="key" collection="list"
             open="" separator="," close="">(#{item.userName}, #{item.password})
    </foreach>
-->



    <!--<resultMap id="orderItem-product" type="com.neuedu.entity.Cart" autoMapping="true">
        <id property="cartid" column="cartid"></id>

        <association property="product" column="product_id" javaType="com.neuedu.entity.Product" autoMapping="true">

            <id property="productid" column="productid"></id>


        </association>


    </resultMap>
    <select id="addOrderItem" resultMap="orderItem-product">
insert into orderItem(user_id, orderItem_no,product_id,product_name,product_image,current_unit_price,quantity,total_price,create_time) values(#{user_id},#{orderItem_no},#{product_id},#{product_name},#{product_image},#{current_unit_price},#{quantity},#{total_price},now())
    </select>-->


</mapper>